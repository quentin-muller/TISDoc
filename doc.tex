\section{applyLUT}
\begin{lstlisting}[style=docstringstyle]

    Applies LUT to an image
    
    Parameters:
    -----------
        f   : image
        lut : lookup table
    Returns:
    -----------
        new image
    
\end{lstlisting}
\section{computeCumulativeHisto}
\begin{lstlisting}[style=docstringstyle]

    Computes cumulative histogram from base histrogram
    
    Arguments:
        h : histogram
        
    Return:
        cumulative histogram,
        cumulative normalized histogram
    
\end{lstlisting}
\section{find\_nearest}
Pas de docstring
\section{halfToning}
\begin{lstlisting}[style=docstringstyle]

    Applies halftoning algorithm to f and returns the results as 2D array
    
    Arguments:
        img : image
    Return:
        h : new image
    
\end{lstlisting}
\section{imgLevelAdjust}
\begin{lstlisting}[style=docstringstyle]

    Adjusts image's contrast
    Arguments:
        f     : image
        _mini : lower percentage limit (default 1)
        _maxi : higher percentage limit (default 99)
        
    Return:
        adjusted image
    
\end{lstlisting}
\section{showHistogram}
\begin{lstlisting}[style=docstringstyle]
Displays histograms and its cumulative
    n <------
    Arguments
    h : histogramme
    
    Return
    fig : figure
    axs : axis
    
    
\end{lstlisting}
\section{showImage}
Pas de docstring
